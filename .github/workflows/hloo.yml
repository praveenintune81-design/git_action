---
name: Hello Praveen
on:
  push:
    branches:
      - main
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: say hello tp users
        run: echo "hello praveen"
      - name: say hello to username
        run: echo "hello $USER"
